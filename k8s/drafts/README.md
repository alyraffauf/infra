# Draft charts

These charts are deliberately not reconciled by Flux. They are retained as
work-in-progress manifests, not deployable production state. Move a chart back
to `k8s/charts/` and add exactly one HelmRelease before deploying it.

- `audiobookshelf`
- `forgejo`
- `jellyfin`
