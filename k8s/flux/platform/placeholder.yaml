apiVersion: v1
kind: ConfigMap
metadata:
  name: flux-layer-platform-placeholder
  namespace: flux-system
  labels:
    cute.haus/flux-layer-placeholder: "true"
data:
  note: Remove this placeholder when platform has real resources.
