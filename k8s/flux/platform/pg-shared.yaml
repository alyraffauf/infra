apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pg-shared
  namespace: flux-system
spec:
  interval: 15m
  releaseName: pg-shared
  targetNamespace: cnpg-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: ./k8s/charts/pg-shared
      interval: 15m
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: cute-haus-global
      valuesKey: values.yaml
  values:
    backup:
      enabled: true
    roles:
      # Cleanup tombstone: remove after CNPG reports the database and role absent.
      forgejo:
        database: forgejo
        ensure: absent
      tranquil:
        database: tranquil
      pocket-id:
        database: pocket-id
      paperless:
        database: paperless
      nextcloud:
        database: nextcloud
