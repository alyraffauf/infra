apiVersion: v1
kind: ConfigMap
metadata:
  name: flux-layer-apps-placeholder
  namespace: flux-system
  labels:
    cute.haus/flux-layer-placeholder: "true"
data:
  note: Remove this placeholder when apps has real resources.
