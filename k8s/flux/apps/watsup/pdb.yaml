apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: watsup
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: watsup
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: watsup
