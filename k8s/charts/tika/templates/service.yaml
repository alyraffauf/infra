apiVersion: v1
kind: Service
metadata:
  name: tika
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: tika
spec:
  type: ClusterIP
  selector:
    app: tika
  ports:
    - name: http
      port: 9998
      targetPort: 9998
