apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uptime-kuma
  labels:
    app: uptime-kuma
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "kuma.cute.haus"
      secretName: cute-haus-tls
    - hosts:
        - "status.cute.haus"
      secretName: cute-haus-tls
    - hosts:
        - "status.aly.social"
      secretName: aly-social-tls
  rules:
    - host: "kuma.cute.haus"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: uptime-kuma
                port:
                  number: 80
    - host: "status.cute.haus"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: uptime-kuma
                port:
                  number: 80
    - host: "status.aly.social"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: uptime-kuma
                port:
                  number: 80
