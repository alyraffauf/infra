apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: morsels
  labels:
    app: morsels
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "morsels.blue"
        - "www.morsels.blue"
      secretName: morsels-blue-tls
  rules:
    - host: "morsels.blue"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: morsels
                port:
                  number: 80
    - host: "www.morsels.blue"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: morsels
                port:
                  number: 80
