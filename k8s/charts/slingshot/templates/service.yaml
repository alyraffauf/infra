apiVersion: v1
kind: Service
metadata:
  name: slingshot
  labels:
    app: slingshot
spec:
  type: ClusterIP
  selector:
    app: slingshot
  ports:
    - name: http
      port: 80
      targetPort: http
