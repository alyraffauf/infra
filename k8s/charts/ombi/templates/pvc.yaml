apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ombi-data
  labels:
    app: ombi
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
