apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config
  annotations:
    helm.sh/resource-policy: keep
  labels:
    app: {{ .Chart.Name }}
data:
  watsup.toml: |
{{ .Files.Get "config.toml" | indent 4 }}
