oauth:
  clientId: ref+sops://secrets/tailscale-operator.yaml#/secret/CLIENT_ID
  clientSecret: ref+sops://secrets/tailscale-operator.yaml#/secret/CLIENT_SECRET

operatorConfig:
  defaultTags:
    - tag:k8s
