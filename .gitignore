.DS_Store
.direnv/
.zed/
result

# OpenTofu state lives in B2, but local state and plans may still appear.
# The provider lock file stays tracked so every machine resolves the same
# provider versions.
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfplan
